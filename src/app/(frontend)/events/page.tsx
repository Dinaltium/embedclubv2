import { SidebarShell, MainbarShell } from "@/components/FrontendShell";

export default async function Page() {
  return (
    <SidebarShell>
      <MainbarShell>
      </MainbarShell>
    </SidebarShell>
  );
}
